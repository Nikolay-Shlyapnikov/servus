import {Input} from "../../shared/UI/input/input";

const ParamForm = () => {
    return (
        <form>
            <Input name={'URL'} value={''} changeInputFunc={() =>{}}/>
        </form>
    );
};

export default ParamForm;